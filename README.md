1. Install dependencies:
   `npm install`
3. Run the app:
   `npm run dev`


Initial commit

<!-- to check repo link: git remote -v
git add.
git commit -m "..."
git push origin main
npm run deploy -->